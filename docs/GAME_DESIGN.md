# GAME_DESIGN — おじさんクエスト（ojisan-quest）骨子

> ⚠️ **最新の正本は `STORY.md`（物語）／`GAME_PLAN.md`（構成・システム・ロードマップ）／`ART_ORDERS.md`（発注）。**
> 本ファイルは初期の“種”であり一部に旧記述が残る（例：下表の「タイルセット/背景3〜4種」は、確定方針＝**軽量通路・タイルマップ不使用・背景5枚** に置き換わった。NPC「店主」は **のりちゃん（ママ・女性）** が確定）。矛盾時は STORY/GAME_PLAN を優先。
> オリジナルの**シンプルなターン制コマンドRPG**。特定作品のパロディではない。作りやすさ最優先。
> 共通ルール：有名作の固有キャラ/名称/ロゴ/音楽/見た目は使わない（全部おじさんオリジナル）。

## ジャンル / コアループ
- ターン制コマンドバトルRPG（正面ビュー・行動順は素早さ順・ATBなし＝実装が単純で堅牢）。
- ループ：フィールド移動 → エンカウント → コマンド戦闘（たたかう／とくぎ／どうぐ／にげる）→ 経験値/お金/アイテム → 町で補給 → ダンジョン → ボス。

## 作りやすさのための最小構成（まずここを動かす）
- **主人公おじさん1人パーティ**で開始（仲間は後で足せるデータ構造に）。
- 敵は前方に静止スプライト表示。被弾でフラッシュ、撃破でフェードの簡易演出。
- HP/MP・レベル・経験値・お金・基本コマンド・1〜2種のとくぎ・回復どうぐ、まで。
- セーブは最初はチェックポイント（拠点）方式で十分。

## 世界観（流用前提・コミカル哀愁）
- **物語前提（確定 2026-06-27）＝「定時に帰れない呪い」**：残業の夜、おじさんは謎の力で会社ビルに
  閉じ込められた。出口は消え、エレベーターは見知らぬフロアへ。各階に巣食う“残業の魔物”
  （魔物化した社員・役職者）を倒し、**定時退社＝ビルからの脱出**を目指す。コミカル哀愁トーン。
- 主人公：おじさん（A案）。
- 敵：既存ロスター流用（イモムシ社員・果物メガネ女子・バナナ筋肉女子・大仏豚ボス 等）。
- 王道RPGの役割を“おじさん読み替え”でオリジナル化（拠点＝喫茶店、強敵＝役職者、魔王城＝最上階の社長室）。

## 流用 vs 新規
| 区分 | 流用（発注不要） | 新規発注 |
|---|---|---|
| キャラ | 主人公 idle/attack/hurt、敵ロスター、アイテム | NPC（のりちゃん(ママ)/受付/守衛/"社長"）、追加敵、中/ラスボス |
| 画面 | — | バトル背景5枚（フロア別）、通路用の簡易背景、UI（コマンド枠/HPバー/アイコン）。※タイルマップは不使用 |

> 詳細な発注一覧は `ART_ORDERS.md` を正とする（この表は概観）。

## データ駆動の単位（`scaffold/content.example.js` の RPG 例に沿う）
- `ENEMIES`（hp/atk/def/exp/gold/skills/sprite）, `SKILLS`（kind/power/target/element）,
  `ENCOUNTERS`（エリア→敵編成/出現率）, `DIALOGUE`/`SCENARIO`, `ITEMS`。エンジンは薄く、内容はデータで増やす。

## ★決めること（open questions）
1. ~~**物語の前提**~~ → **確定（2026-06-27）：定時に帰れない呪い**（上記「世界観」）。
2. **戦闘の“一捻り”**（任意・保留）：MVPは王道のまま。動いてから検討（例：弱点=おじさんの本音／満員電車ゲージ／名刺コンボ）。
3. **マップ構造**（未定）：ビル各階＝ダンジョン階層の一本道から小さく。
4. **成長**（未定）：レベル制ベース。装備/とくぎ習得は後。
5. **セーブ**（未定）：拠点（喫茶店）チェックポイント方式から。

## 進捗
- ✅ **最小ループ完成（2026-06-27）**：おじさん1人 vs 敵1体の1戦闘が回る（たたかう／とくぎ／どうぐ／にげる、素早さ順、勝敗→遷移）。
- ✅ **フロア脱出ラン化（2026-06-27・その2）**：1戦闘を**連戦の縦スライス**に拡張。
  `caterpillar→caterpillar→fruitGirl→buddhaPig(ボス)` を順に戦い、**HP/MP・レベル・経験値・お金・持ち物が戦闘をまたいで継続**。
  **EXP/レベルアップを実反映**（レベルアップで全回復＋Lv3で `heavySwing` 習得）。ボス撃破→**脱出エンディング**、力尽き→**ゲームオーバー**（`ResultScene`）。
  戦闘画面にフロア進捗HUD。エンジンは薄いまま、ラン状態は `src/state/run.js`、内容は `src/data/content.js`（`LEVEL_TABLE`/`FLOOR_RUN`）。
- 検証：build通過＋実起動パスを手動ステップで通し確認（Title→4連戦→脱出、レベルLv1→4、コンソールエラーなし）。**実機プレイ＝本人確認待ち**。

## 次の一手
- **本人の実機プレイ**で通しとバランス（敵HP/atk・EXP曲線・テンポ）を確認・調整。
- 画像発注は `docs/ART_ORDERS.md` に準備済み（背景→attack→UI の順で差替）。
- フロアを増やす（`FLOOR_RUN` 複数化 or `ENCOUNTERS` 抽選）。仲間はparty配列化で追加。
- 町（喫茶店拠点）＋セーブ/補給。
